const initialState = {
    homePlace: [-1, "Zerbst"],
    trainingPlace: [1, "w Gostyniu"]
}

const settings = function (state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}

export default settings;